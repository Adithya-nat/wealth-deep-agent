"""A deep agent that turns a brokerage account, a card feed, and an investment
policy into a wealth memo whose every claim can be checked against what the
agent actually observed.

Start at `agents/` for the architecture, `prompts/` for what each agent is told,
and `verify.py` for how the output is checked.
"""

__version__ = "0.2.0"
