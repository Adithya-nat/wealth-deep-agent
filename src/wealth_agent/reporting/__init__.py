"""Turning a finished run into something a person would actually read.

`render` produces one self-contained HTML file in which every figure is
annotated with the ledger entry that grounds it. `delivery` opens it, and
optionally emails it.
"""
